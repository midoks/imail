<div class="four wide column">
	<div class="ui vertical menu">
		<div class="header item">{{.i18n.Tr "settings"}}</div>

		<a class="{{if .PageIsSettingsProfile}}active{{end}} item" href="{{AppSubURL}}/user/settings">
			{{.i18n.Tr "settings.profile"}}
		</a>
	

		<a class="{{if .PageIsSettingsAuthPassword}}active{{end}} item" href="{{AppSubURL}}/user/settings/authpassword">
			{{.i18n.Tr "settings.auth_password"}}
		</a>

		<a class="{{if .PageIsSettingsPassword}}active{{end}} item" href="{{AppSubURL}}/user/settings/password">
			{{.i18n.Tr "settings.password"}}
		</a>

		<a class="{{if .PageIsSettingsPassword}}active{{end}} item" href="{{AppSubURL}}/user/settings/client">
			{{.i18n.Tr "settings.client"}}
		</a>
		
	</div>
</div>
