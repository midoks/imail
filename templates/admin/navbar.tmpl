<div class="four wide column">
	<div class="ui vertical menu">
		<div class="header item">{{.i18n.Tr "admin_panel"}}</div>
		<a class="{{if .PageIsAdminDashboard}}active{{end}} item" href="{{AppSubURL}}/admin">
			{{.i18n.Tr "admin.dashboard"}}
		</a>
		<a class="{{if .PageIsAdminDomain}}active{{end}} item" href="{{AppSubURL}}/admin/domain">
			{{.i18n.Tr "admin.domain"}}
		</a>
		<a class="{{if .PageIsAdminUsers}}active{{end}} item" href="{{AppSubURL}}/admin/users">
			{{.i18n.Tr "admin.users"}}
		</a>
		
		<a class="{{if .PageIsAdminConfig}}active{{end}} item" href="{{AppSubURL}}/admin/config">
			{{.i18n.Tr "admin.config"}}
		</a>

		<a class="{{if .PageIsAdminConfig}}active{{end}} item" href="{{AppSubURL}}/admin/log">
			{{.i18n.Tr "admin.log"}}
		</a>

		<a class="{{if .PageIsAdminMonitor}}active{{end}} item" href="{{AppSubURL}}/admin/monitor">
			{{.i18n.Tr "admin.monitor"}}
		</a>
	</div>
</div>
